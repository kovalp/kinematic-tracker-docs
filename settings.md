# Settings

When creating the tracker object we supply parameters to define the layout
of tracked states and consumed detections.
For an example refer to [gentle intro](gentle-intro). 

We define other parameters via setters of the tracker object:

 - [Association metric](association/association-metric)
 - [Matching algorithm](association/matching-algorithm)
